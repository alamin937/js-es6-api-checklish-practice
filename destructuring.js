// work number 6
const student = {
    name: 'kamal',
    id: 1155,
    proffesion: 'student'
}

const {name, id} = student;

console.log(name, id);

// array work
const [first, balance, third] = [55,47,25,65];
console.log(balance);
